# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node
node_modules/

# Python
tests/__pycache__/
__pycache__/
*.pyc
.pytest_cache/

# Logs
*.log

# Local config
.env
.env.local

# Eval test results -- individual per-skill detail files
# Only the merged summary (eval-results.md) and regression_analysis.md are tracked
tests/full-eval-tests/result/eval-*-results.md
tests/full-eval-tests/result/eval-run-telemetry.json
coverage-gap-report.json
coverage-enforcement.json
validation-results.json
security-report.json



# Generated by .github/workflows/quality_checker.py -- produced fresh by CI
quality-report.json

# Vally eval results -- produced by `npx @microsoft/vally-cli eval`
tests/.vally-results/
# Some vally CLI invocations write to a root-level results dir; ignore it too
vally-results/
# Pester writes a JUnit-style testResults.xml at the repo root in CI mode
testResults.xml
# Vally eval staging -- substituted {{WORKSPACE}} copies for real runs (never commit)
tests/.vally-staging/
tests/.vally-baseline-skill-root/
tests/.test-scratch/

# weekly-status-pptx skill -- generated decks and per-week generator copies should
# not be committed (use a gitignored working folder like .local/weekly/)
.local/
*.pptx
Fabric-Skills-Weekly-*.pptx
generate-weekly-*.py
!.github/skills/weekly-status-pptx/generate-weekly-summary.py
